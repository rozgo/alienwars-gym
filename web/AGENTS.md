# Browser and presentation sources

The root AGENTS.md applies. Read [WEB.md](../docs/WEB.md) for browser builds.
Edit Map Lab in `maplab/shell.html`; `../docs/maplab/` is generated output.
Use `uv run scripts/build_fleet_site.py` from the root to retain verified policies.

Keep relative URLs for the GitHub Pages project subpath. Preserve camera pointer
capture, wheel behavior, seed query parameters and equipment/overlay separation.
For an interface-only change, inspect actual Chrome behavior and use the root
validation table. Do not replace a trained build with a reference-controller build.
